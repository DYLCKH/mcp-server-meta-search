name: Release Binaries

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (e.g. v1.0.0)"
        required: false

permissions:
  contents: write

env:
  BUN_VERSION: "1.3.6"

jobs:
  build-metadata:
    name: Resolve Build Metadata
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v5

      - name: Resolve build version
        id: version
        run: |
          VERSION="$(node ./scripts/resolve-build-version.mjs)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Resolved build version: $VERSION"

  build-web:
    name: Build Web Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - run: bun install --frozen-lockfile

      - run: bun run --filter @meta-search/web build

      - uses: actions/upload-artifact@v5
        with:
          name: web-dist
          path: apps/web/dist/
          retention-days: 1

  build-binary:
    name: Build ${{ matrix.label }}
    needs:
      - build-metadata
      - build-web
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: bun-linux-x64
            runner: ubuntu-latest
            label: linux-x64
            artifact: meta-search-linux-x64
          - target: bun-linux-arm64
            runner: ubuntu-latest
            label: linux-arm64
            artifact: meta-search-linux-arm64
          - target: bun-darwin-arm64
            runner: macos-14
            label: darwin-arm64
            artifact: meta-search-darwin-arm64
          - target: bun-windows-x64
            runner: ubuntu-latest
            label: windows-x64
            artifact: meta-search-windows-x64.exe

    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - run: bun install --frozen-lockfile

      # Download pre-built web frontend
      - uses: actions/download-artifact@v5
        with:
          name: web-dist
          path: apps/web/dist/

      # Build workspace packages
      - run: bun run --filter @meta-search/shared build
      - run: bun run --filter @meta-search/config build
      - run: bun run --filter @meta-search/runtime build

      # Generate embedded assets and config example
      - run: bun ./scripts/embed-assets.mjs
      - run: bun ./scripts/embed-config-example.mjs

      # Compile binary
      - env:
          META_SEARCH_BUILD_VERSION: ${{ needs.build-metadata.outputs.version }}
        run: |
          mkdir -p dist-bin
          bun build --compile --target=${{ matrix.target }} \
            --compile-exec-argv=--smol \
            --env=META_SEARCH_BUILD_* \
            --outfile dist-bin/${{ matrix.artifact }} \
            apps/server/src/index.ts

      - uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.artifact }}
          path: dist-bin/${{ matrix.artifact }}
          retention-days: 5

  release:
    name: Create Release
    needs:
      - build-metadata
      - build-binary
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v5
        with:
          path: artifacts/
          merge-multiple: false

      - name: Prepare release assets
        run: |
          mkdir -p release
          VERSION="${{ needs.build-metadata.outputs.version }}"
          for dir in artifacts/meta-search-*; do
            name=$(basename "$dir")
            cp "$dir/$name" "release/$name"
            chmod +x "release/$name"
            printf '%s\n' "$VERSION" > "release/$name.version"
            cd release && sha256sum "$name" > "$name.sha256" && cd ..
          done
          printf '%s\n' "$VERSION" > release/version.txt
          cp release/version.txt release/versions.txt
          cp release/version.txt release/versons.txt
          ls -lh release/

      - uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true

  release-dev:
    name: Rolling Dev Release
    needs:
      - build-metadata
      - build-binary
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    concurrency:
      group: release-dev
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v5
        with:
          path: artifacts/
          merge-multiple: false

      - name: Prepare release assets
        run: |
          mkdir -p release
          VERSION="${{ needs.build-metadata.outputs.version }}"
          for dir in artifacts/meta-search-*; do
            name=$(basename "$dir")
            cp "$dir/$name" "release/$name"
            chmod +x "release/$name"
            printf '%s\n' "$VERSION" > "release/$name.version"
            cd release && sha256sum "$name" > "$name.sha256" && cd ..
          done
          printf '%s\n' "$VERSION" > release/version.txt
          cp release/version.txt release/versions.txt
          cp release/version.txt release/versons.txt
          ls -lh release/

      - name: Remove previous dev release and tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh release delete dev --yes --cleanup-tag || true

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: dev
          name: ${{ needs.build-metadata.outputs.version }}
          target_commitish: ${{ github.sha }}
          prerelease: true
          make_latest: "false"
          files: release/*
          body: |
            Rolling pre-release built from `main` (${{ github.sha }}).

            - Build: `${{ needs.build-metadata.outputs.version }}`
            - Commit: ${{ github.sha }}
            - Run: ${{ github.run_number }}
